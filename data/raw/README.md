Intentionally empty in this repo. Raw scrape data comes from three separate
third-party GitHub repos with their own licenses (see top-level README for
names/years). `src/data_pipeline/consolidate.py` documents and reproduces the
unification logic; the verified output is checked in at
`data/processed/josaa_iit_consolidated.csv`.
